package Models;

public class ComplexModel extends Model {

    public ComplexModel(double startX, double sizeX, double startY, double sizeY, double price, double priority) {
        super(startX, sizeX, startY, sizeY, price, priority);
    }
}
